name: End-to-end tests
on:
  push:
    branches:
      - master
      - sftp-permission
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v3
    
      - name: Custom tests 🧪
        uses: cypress-io/github-action@v5
        with:
          command: npm run record:test
